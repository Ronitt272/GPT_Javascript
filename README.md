# GPT_Javascript

To run:
`npm init` \\
`npm install express` \\
`pip install transformers` \\
`pip install torch` \\
`pip install requests` \\ 
`npm install child-process` \\
`node server.js` \\
`node run_python.js` 
